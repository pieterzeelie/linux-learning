## 🛠️ Practical Exercises

### 1. File Listing
```bash
# Linux (Bash)
ls -l --color

# UNIX (older Bourne shell)
ls -l

## 📦 Package management 

# Linux (Ubuntu/Debian)
sudo apt-get install nginx

# UNIX (Solaris)
pkgadd -d nginx.pkg

## ⚙️ Process management

# Linux
ps aux | grep python

# UNIX
ps -ef | grep python

